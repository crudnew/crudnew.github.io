.DS_Store
assets/.DS_Store
.claude/
CLAUDE.md
assets/UofT-Logo.png
assets/MacEwan-Logo.png

# Local backups of original PNGs before WebP conversion — kept for safety,
# not needed in repo.
assets/*-original.png

# Local-only dev server (clean-URL fallback for localhost). Dev tool, not deployed.
devserver.py

# Claude Code local memory — not for public repo
memory/
